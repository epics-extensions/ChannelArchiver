# Production html browser (shelp is a script that
# executes chimera).  This could also be set to
# netscape if supported.
setenv BROWSER        shelp

# The following environment variables are used by the Archive
# Browser.  If not specified, the default for a path environment
# variable is the current directory.
# 
#   1. ARDATAFILES      - specifies the base path containing the data files.
#   2. ARBLOGFILES      - specifies the base path containing log files generated
#                         by the browser.  If specified, it is assumed that subdirectories
#                         with names the same as the months of the year (e.g., "January",
#                         "February", "March", etc.) exist under the base directory.
#                         Log information is stored in the subdirectory with the name
#                         of the current month if this environment variable is specified.
#   3. ARBMATLABFILES   - specifies  the path where Matlab files generated by users
#                         are stored.
#   4. ARBPSFILES       - specifies the path where Postscript files generated by users
#                         are stored.
#   5. ARBCONFIGFILES   - specifies the path where Configuration files saved by users
#                         are stored.
#   6. ARBCONFIGEXT     - specifies the default configuration file extension.  The
#                         default is "cfg".
#   7. ARBROWSER        - specifies the path and name of the Browser executable file.
#
setenv ARDATAFILES    ${CD_DATA}/archive/pepii/data
setenv ARBLOGFILES    ${CD_DATA}/archive/pepii/log
setenv ARBMATLABFILES ${CD_DATA}/archive/pepii/matlab
setenv ARBPSFILES     ${CD_DATA}/archive/pepii/ps
setenv ARBCONFIGFILES ${CD_DATA}/archive/pepii/config
setenv ARBCONFIGEXT   cfg
setenv ARBROWSER      ${EPICS}/extensions/src/archiveBrowser
